SELECT *
FROM (
    SELECT SHIPMENT_ID, FLAVOR, TOTAL_ORDER
    FROM FIRST_HALF
    UNION ALL -- JOIN이 아닌 UNION을 통해 계산
    SELECT SHIPMENT_ID, FLAVOR, TOTAL_ORDER
    FROM JULY
) AS TMP
GROUP BY FLAVOR
ORDER BY SUM(TOTAL_ORDER) DESC
LIMIT 3;
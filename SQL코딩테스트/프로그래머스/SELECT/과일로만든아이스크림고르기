SELECT fh.FLAVOR
FROM FIRST_HALF fh
LEFT JOIN ICECREAM_INFO ii -- LEFT JOIN 방법
ON fh.FLAVOR = ii.FLAVOR -- LEFT JOIN 방법
WHERE fh.TOTAL_ORDER > 3000 AND ii.INGREDIENT_TYPE = 'fruit_based'
ORDER BY fh.TOTAL_ORDER DESC;